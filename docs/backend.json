
{
  "entities": {
    "BookListing": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BookListing",
      "type": "object",
      "description": "Represents a book listing created by a student.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BookListing entity."
        },
        "sellerId": {
          "type": "string",
          "description": "Reference to the User selling the book."
        },
        "title": {
          "type": "string",
          "description": "Title of the book."
        },
        "author": {
          "type": "string",
          "description": "Author of the book."
        },
        "isbn": {
          "type": "string",
          "description": "ISBN of the book."
        },
        "condition": {
          "type": "string",
          "description": "Condition of the book (e.g., New, Used - Like New, Used - Good, Used - Acceptable)."
        },
        "gradeLevel": {
          "type": "string",
          "description": "Grade level for which the book is intended (e.g., 9th, 10th, 11th, 12th).",
          "enum": ["8", "9", "10", "11", "12"]
        },
        "price": {
          "type": "number",
          "description": "Price of the book."
        },
        "description": {
          "type": "string",
          "description": "A more detailed description of the book for sale."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the book's image."
        },
        "listingCharge": {
          "type": "number",
          "description": "Optional fee to promote the listing."
        },
        "deliveryMethod": {
            "type": "string",
            "description": "How the book will be delivered.",
            "enum": ["Pickup Point", "Team Handling"]
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp when the listing was created."
        },
        "updatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp when the listing was last updated."
        }
      },
      "required": [
        "sellerId",
        "title",
        "condition",
        "gradeLevel",
        "price",
        "createdAt",
        "updatedAt",
        "deliveryMethod"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "username": {
          "type": "string",
          "description": "Username of the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "Full name of the user."
        },
        "contactNumber": {
          "type": "string",
          "description": "Contact number of the user."
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents a message between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Message entity."
        },
        "senderId": {
          "type": "string",
          "description": "Reference to the User who sent the message."
        },
        "receiverId": {
          "type": "string",
          "description": "Reference to the User who received the message."
        },
        "bookListingId": {
          "type": "string",
          "description": "Reference to the BookListing the message is about."
        },
        "content": {
          "type": "string",
          "description": "Content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the message was sent.",
          "format": "date-time"
        }
      },
      "required": [
        "senderId",
        "receiverId",
        "bookListingId",
        "content",
        "timestamp"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Represents a completed transaction between a buyer and a seller.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transaction entity."
        },
        "bookListingId": {
          "type": "string",
          "description": "Reference to the BookListing that was sold."
        },
        "buyerId": {
          "type": "string",
          "description": "Reference to the User who bought the book."
        },
        "sellerId": {
          "type": "string",
          "description": "Reference to the User who sold the book."
        },
        "transactionDate": {
          "type": "string",
          "description": "Date and time of the transaction.",
          "format": "date-time"
        },
        "price": {
          "type": "number",
          "description": "Price at which the book was sold."
        },
        "status": {
          "type": "string",
          "description": "Status of the transaction (e.g., Completed, Refunded)."
        }
      },
      "required": [
        "bookListingId",
        "buyerId",
        "sellerId",
        "transactionDate",
        "price",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profile information."
    },
    "/bookListings/{bookListingId}": {
      "schema": {
        "$ref": "#/entities/BookListing"
      },
      "description": "Stores all book listings from all users."
    },
    "/bookListings/{bookListingId}/messages/{messageId}": {
      "schema": {
        "$ref": "#/entities/Message"
      },
      "description": "Stores messages related to a specific book listing."
    },
    "/transactions/{transactionId}": {
      "schema": {
        "$ref": "#/entities/Transaction"
      },
      "description": "Stores transaction records."
    }
  }
}
